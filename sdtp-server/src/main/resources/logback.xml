<?xml version="1.0" encoding="UTF-8"?>
<configuration scan="true" scanPeriod="30 seconds">

    <!-- 日志目录：可通过 -Dlog.dir=/path/to/logs 覆盖，默认为启动目录下的 logs/ -->
    <property name="LOG_DIR" value="${log.dir:-logs}" />
    <property name="LOG_FILE" value="${LOG_DIR}/proxy.log" />

    <!-- ==================== 控制台输出（foreground/Docker） ==================== -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- ==================== 文件输出（按天滚动，保留30天） ==================== -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_FILE}</file>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
            <charset>UTF-8</charset>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOG_DIR}/proxy.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>30</maxHistory>
        </rollingPolicy>
    </appender>

    <!-- Netty 自身日志调为 WARN，避免刷屏 -->
    <logger name="io.netty" level="WARN" />

    <!-- Proxy 日志：INFO -->
    <logger name="com.translator.proxy" level="DEBUG" />
    <logger name="com.translator.core" level="INFO" />

    <!-- ==================== SQL 翻译隔离日志输出 ==================== -->
    <appender name="SQL_TRANS" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_DIR}/sql_trans/sql_trans.log</file>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
            <charset>UTF-8</charset>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOG_DIR}/sql_trans/sql_trans.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>30</maxHistory>
        </rollingPolicy>
    </appender>

    <!-- SQL 翻译审计日志隔离配置 -->
    <logger name="sql-translate-record" level="INFO" additivity="false">
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="SQL_TRANS" />
    </logger>

    <!-- 根日志 -->
    <root level="INFO">
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="FILE" />
    </root>

</configuration>
